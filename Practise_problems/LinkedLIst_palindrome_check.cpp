#include<bits/stdc++.h>
using namespace std;
int main()
{
  list<int>ll;
  int val;
  while(1)
  {
    cin>>val;
    if(val==-1)
    {
      break;
    }
    ll.push_back(val);
  } 
  for(auto  i=ll.begin(),j=ll.end();)

  return 0;
}